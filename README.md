# Maya Prompt Studio Verified

已通过本地 `npm install` 和 `npm run build` 验证的正式可部署版本。

## 部署

1. 解压 zip
2. 上传根目录所有文件到 GitHub
3. Vercel Import 仓库
4. 点击 Deploy

## 结构要求

GitHub 根目录必须直接看到：

- package.json
- app
- components
- lib
- dashboard
- supabase

不要多套一层文件夹。
